name: Frontend React Lint

on:
  pull_request:
    branches: [main, sub-new]
  push:
    branches: [main, sub-new]

permissions:
  contents: read
  pull-requests: write

jobs:
  frontend-lint:
    name: React Lint & Build Check
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: frontend

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20" # Node 20 is required for ESLint 9 compatibility
          cache: "npm"
          cache-dependency-path: frontend/package.json

      - name: Install dependencies
        run: npm ci

      - name: Run ESLint with Reviewdog
        uses: reviewdog/action-eslint@v1
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          # This reporter ensures it works on both Push and PR
          reporter: ${{ github.event_name == 'pull_request' && 'github-pr-review' || 'github-check' }}
          workdir: frontend
          filter_mode: nofilter
          fail_level: error
          # Updated flags for ESLint 9 compatibility
          eslint_flags: "src/"
